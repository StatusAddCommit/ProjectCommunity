const templateReducer = (state = [], action) => {
  switch (action.type) {
    case 'TEMPLATE_ACTION':
      // replace me
      return state;
    default:
      // replace me
      return state;
  }
};

export default templateReducer;
